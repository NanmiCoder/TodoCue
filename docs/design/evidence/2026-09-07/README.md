# 独立视觉审稿截图

2026-09-07 使用原生 CUA 截图接口采集，共 11 张。截图保持接口返回的原图，没有重绘、修图或增强材质。

- `01`：`/Applications/TodoCue.app`，真实已完成空状态，浅色，340 × 680。
- `02`–`08`：与当前 UI 同源的 debug 预览，隔离数据目录，浅色，文件名标明 340 或 300 宽度，均高 680。
- `09`–`11`：相同 UI 与隔离任务，强制 App 深色外观，文件名标明宽度，均高 680。
- `07` 是编辑页向下滚动后的状态；`11` 是快速输入获得焦点后的状态。
- 隔离任务包含中文长标题、项目、备注、日期、截止时间与预计耗时。没有为截图修改用户的真实任务。
- 深色截图仅代表当前桌面背景下的合成结果，不能据此断言所有背景下的材质表现。系统可见滚动条也保留原样。
- 这一轮证据用于视觉审稿，没有覆盖动画全程、键盘和辅助功能全流程、通知投递或所有任务规模。

两位审稿人分别阅读界面源码并使用 `view_image` 查看截图，形成结论前未互看报告，未阅读项目已有设计评价。
